data:
holds input data to the algorithms (e.g. MNIST data).

.gitignore file has mnist.pkl.gz mentioned, so it is not cloned/pushed/pulled from GitHub.
One can soft/hard link it from previous experiments in this directory or otherise rbm.py will
download it here when run first time.

