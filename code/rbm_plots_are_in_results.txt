rbm_plots*/ and rbm_data*/ directories are results created by rbm.py
The right place for these would be ../results/ directory, so they
are moved there and code had been updated so it creates them there.


